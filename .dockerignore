# Node & package managers
node_modules
.pnp
.pnp.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Yarn Berry: kirim hanya yang diperlukan (releases/plugins/versions/patches)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# Jika Anda menyimpan plugin custom lain, whitelist di sini:
# !.yarn/sdks

# Next.js build artifacts
.next
out
build

# Tests/coverage
coverage

# Environment files (jangan kirim ke Docker build context)
.env
.env.*
!.env.example

# Vercel
.vercel

# OS/editor files
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts
.vscode
.idea

# Git metadata
.git
.gitignore
.gitattributes

# Docker local files (opsional, biasanya tidak perlu di context)
docker-compose*.yml
