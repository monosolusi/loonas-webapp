/**
 * `account.hasFeature()` flag gating the "Sesuaikan Stok" action. Shared by
 * every stock item surface (Penyesuaian Stok list, Stok Negatif list, product
 * and raw material detail stock cards) so the flag string has one owner.
 */
export const INVENTORY_ADJUSTMENT_FEATURE = "inventory_adjustment";
