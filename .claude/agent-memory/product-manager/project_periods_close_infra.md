---
name: project-periods-close-infra
description: LNS-377 periods/close/reopen SHIPPED (PR#90, In Review); final contract (start_at/end_at/closed_by, status open|closed|locked, can_close/can_reopen + dual PPH_FINAL_NOT_POSTED landed mid-build); v1=graceful-403 degrade; DEFER-2 enhancement→LNS-378, DEFER-3=LNS-404
metadata:
  type: project
---

LNS-377 builds the `/finance/periods` surface (list + monthly close/reopen) on the already-shipped BE contract `GET/POST /accounting/periods[/{id}][/close|/reopen]`. Shared periods domain/data infra is consumed by LNS-378 (year-end) and LNS-354 (fixed-costs guard).

**Why:** Books-locking is a high-stakes fintech control; FE had no surface to drive the BE close/reopen mechanism (LNS-102).

**SHIPPED (LNS-377 PR #90 In Review 2026-06-25). CONTRACT REALITY — verified against the FINAL live spec + shipped FE model:**
- **`AccountingPeriod` fields (BE RENAMED 3 mid-build, post-rename truth):** `id, account_id, kind, start_at (date-time), end_at (date-time), status, closed_by (nullable uuid), closed_at (nullable), created_at`. (Earlier Phase-2 scan saw `start_date`/`end_date`/`closed_by_user_id` — those were renamed; EL re-validated `fromJson` key-by-key.) Row label derives from `start_at`→`DateTime.fromISO` — a wrong field name fails AC-1 SILENTLY (blank/"Invalid DateTime", no crash); this is the one thing tsc/lint can't catch, so confirm field names against the live spec on any future period-shape change. NO `reason`/audit-history array (posted reasons are write-only).
- **`status` enum (BE ADDED a 3rd mid-build): `open`, `closed`, `locked`** (`locked` = year-locked). FE `isClosed = status !== "open"` → any non-open value maps to Terkunci/success defensively, never crashes. reopen-409 currently maps any 409 → "masih terbuka" (imprecise for `locked`; LNS-378 owns the precise year-unlock UX = DEFER-1). **`kind` = `["month"]`** (single; year-level = LNS-378).
- **Pagination = `page`/`limit`** (symmetric, default 25, max 100, `meta:{page,limit,total,total_pages}`). NO offset trap (unlike LNS-386 journals).
- **Idempotency-Key required on both** (`^[A-Za-z0-9_-]{8,255}$`; `crypto.randomUUID()` satisfies). **close `reason` BE-OPTIONAL (maxLength 500); reopen `reason` BE-REQUIRED (minLength 10, maxLength 500).** FE enforces reason required on BOTH for audit consistency. close/reopen 200 return the full updated period → chip flips from the mutation response (revalidate list key for cache only). Re-mint a FRESH key on retry — BE caches 4xx/5xx under the key (cached-failure replay).

**✅ LNS-397 RESOLVED (reframe — supersedes the earlier "ABSENT" flag):** the capability signal + enriched PPh-Final contract were ABSENT in the Phase-2 scan but **DID LAND MID-BUILD**. BE shipped `can_close`/`can_reopen` booleans on the period + a **dual-natured `PPH_FINAL_NOT_POSTED`** (2xx pre-deadline advisory with `warnings[]` incl. account 8110 / setor_deadline / DPP / regime, AND 422 post-deadline block). **Dependency SATISFIED, not outstanding.** v1 FE shipped the graceful-403 degrade and does NOT yet consume these (DEFER-2 enhancement: pre-compute gating from the booleans + render the pre-deadline advisory richly — likely folded into LNS-378). No BE follow-up ticket needed (the contract is there). **DEFER-3 filed = LNS-404** (FE-internal cleanup: accounting use-case/source import-repo-params, mirrors `journal.ts`; Frontend+Improvement).

**v1 SCOPE DECISION (precedent call, accepted 2026-06-22):** ship the **graceful-403 + untyped-422 degrade** — show close/reopen to all `hasFeature("accounting")` users, enforce auth via BE 403 → calm Bahasa; show one calm combined 422 message (best-effort structured if `details.code` arrives). This is the SAME degrade already shipped for **LNS-380 tax-posture** (`src/app/(authenticated)/settings/tax-posture/_providers/tax-posture-provider.tsx:210` handles `ErrorCodes.FORBIDDEN` → `tax-posture-access-denied.tsx`; no per-actor capability primitive). Functionally safe: BE still hard-enforces. NOT a re-block. See [[feedback-server-validation-field-level-gated-by-error-schema]].

**Build facts:**
- **No FE capability/permission primitive** in `src/features/` or `src/core/` (grep-empty 2026-06-25). Gating source is feature-flag + BE-403, not an FE primitive.
- **No FE consumer for `/accounting/periods`** yet — net-new entity/model/repo/source/usecases/hooks. Model the page on shipped LNS-380 `settings/tax-posture/` (page+_providers+_components, hand-rolled useState/useMemo, useSWRMutation, revalidateSWRKey). Idempotency precedent: `src/app/(pos)/pos/_providers/pos-provider.tsx` mints fresh `crypto.randomUUID()` and RE-MINTS before retry (BE caches 4xx/5xx under the key → reuse replays the failure); header passthrough precedent at in-feature `journal.ts:52` — no HttpRequest change needed. See [[manual-journal-idempotency]].
- **UID locked:** Terkunci=`StatusChip variant="success"`, Terbuka=`variant="neutral"` (both present in `src/core/presentations/components/status-chip.tsx`); close confirm=`PrimaryButton` (not Danger); PPh-Final=inline `bg-warning-50 border-warning-400` callout, submit stays enabled for external-resolve retry; base=`LoonasDialog width="sm"` + `DialogFooter` (reason field between body/footer; not `ConfirmationDialog`).
- Route prefix `/finance/...`; nav in `finance-nav-group.tsx`; ROUTE_MAP in `header-title.tsx`. See [[finance-nav-ia]], [[accounting-list-pagination-drift]].
