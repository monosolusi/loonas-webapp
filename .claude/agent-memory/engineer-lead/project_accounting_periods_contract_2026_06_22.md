---
name: accounting-periods-contract-2026-06-22
description: LNS-377 verified — /accounting/periods list/get/close/reopen contract, AccountingPeriod schema (no label/reason field), page-IN/page-OUT, Idempotency-Key required, no capability field, generic Error 422
metadata:
  type: project
---

LNS-377 feasibility pass (2026-06-22) verified against live `https://dev-api.loonas.id/openapi.json` (raw JSON parse). Net-new FE work — no AccountingPeriod domain/data/entity/model exists in `src/features/accounting`. Periods infra is shared with LNS-378 year-end; design reusable.

**`AccountingPeriod` schema (verbatim):** `id` (uuid), `account_id` (uuid), `kind` enum `["month"]` (single member), `start_date` (date), `end_date` (date), `status` enum `["open","closed"]` (clean 2-state, NO transitional — Terbuka=open, Terkunci=closed), `closed_by_user_id` (uuid, nullable), `closed_at` (date-time, nullable), `created_at` (date-time).
- **NO month/period label field** → derive display label ("Mei 2026") from start_date/end_date via Luxon (id locale).
- **NO reason field returned** → the `reason` POSTed on close/reopen is write-only, never echoed on the period object.

**GET /accounting/periods** (`listAccountingPeriods`): query `status` enum `[open,closed]`, `kind` enum `[month]`, `page` (min 1, default 1), `limit` (min 1, **max 100**, default 25). Response `{data, meta}`, `meta` = `PaginationMeta` page/limit/total/total_pages. **Symmetric page-IN/page-OUT** — does NOT repeat the LNS-386 journal-list offset-IN trap; use the standard page-based sibling pattern (`/accounting/accounts`, `coa-mappings`, `fixed-cost-entries`), NOT journal.ts offset conversion. See [[journal-list-param-drift]].

**GET /accounting/periods/{id}** (`getAccountingPeriod`): 200 AccountingPeriod, 404 NOT_FOUND.

**POST /accounting/periods/{id}/close** (`closeAccountingPeriod`): `Idempotency-Key` header **required: true** (pattern `^[A-Za-z0-9_-]{8,255}$`; randomUUID satisfies it). Body `{reason?}` (maxLength 500, optional). 200 AccountingPeriod / 400 (validation or missing key) / 403 (not authenticated OR feature not enabled) / 409 (already closed) / **422 ("Period not drained (pending outbox events) OR PPh Final not posted" — TWO conditions under one 422)**.

**POST /accounting/periods/{id}/reopen** (`reopenAccountingPeriod`): `Idempotency-Key` header **required: true**. Body `{reason}` **required, minLength 10, maxLength 500**. 200 / 400 / 403 / 409 (period not closed — cannot reopen an open period). NO 422.

**Idempotency:** follow POS lifecycle [[pos-idempotency-key-lifecycle]] — fresh key per attempt (page-level provider, lazy useState + regen-on-retry), never reuse across an unwanted 4xx. Plumb via HttpRequest `config.headers` like `invoice.ts:116`. `IDEMPOTENCY_KEY_CONFLICT`/`IDEMPOTENCY_KEY_IN_PROGRESS` already in ErrorCodes.

**Capability gating — NOT spec-answerable.** No per-period capability field, no capabilities/me endpoint, no close/reopen permission flag. Account carries only `role` (free-form string) + `features` (string[] → `hasFeature`), both product-feature enablement not actor-permission. Close/reopen gated purely by 403. PRD's "reopen hidden for non-admins" has no spec source → recommend FE always renders reopen and relies on 403 (v1 default) unless BE supplies a capability/role string. (`membership.ts` isOwner is org-membership, unrelated.)

**422 rich payload — NOT in schema.** All errors `$ref` generic `Error` = `{code, message}` only. The PRD's `PPH_FINAL_NOT_POSTED` rich payload (tenant_regime / expected_account_code "8110" / period_dpp / setor_deadline) is NOT documented anywhere (grep of all schemas: zero pph/setor/regime/dpp error matches) — BE-prose-only, same as LNS-344. HttpRequest now DOES forward `data.details` (see [[opening-balance-contract-2026-06-19]] corrected note), so IF BE sends it, it survives to `serverError.details.details` — but plan must degrade gracefully (generic Bahasa keyed off `code`, opportunistically surface rich fields if present).

**Re-verified 2026-06-25 (LNS-377 Phase-2 fresh fetch, 593KB spec) — drift-corrections to the above:**
- **Single-period `close`/`reopen` error responses name NO codes at all** — bare prose ("Period already closed", "Period not drained (pending outbox events) or PPh Final not posted", "Period not closed"). The DOCUMENTED rich codes (`MONTHLY_PERIODS_NOT_CLOSED`, `YEAR_ALREADY_CLOSED`, `YEAR_NOT_CLOSED`, `YEAR_CLOSE_BOUNDARY_INVALID`, `RETAINED_EARNINGS_ACCOUNT_INVALID`, `YEAR_UNLOCK_TOKEN_MISMATCH`, `PERIOD_NOT_CLOSED`, `IDEMPOTENCY_KEY_REQUIRED`, `PERIOD_REOPEN_REASON_REQUIRED`) live ONLY on the **year-level** endpoints `close-year`/`reopen-year` (LNS-378 scope), in their response `description` Markdown — never in the `Error` schema. So LNS-377's actual codes are spec-unnamed; FE must map by HTTP status + opportunistic `details.code`. ALL these codes are UNREGISTERED in FE `ErrorCodes` today → arrive as `ServerError.code="UNKNOWN"`/httpCode 500, real code at `details.code` (see [[server-error-code-passthrough]]).
- **close/reopen 200 = full `AccountingPeriod`** → FE re-reads `status` (non-null), `closed_at`/`closed_by_user_id` (both nullable, null after reopen) straight from the mutation response. No separate refetch for the chip flip; revalidate the LIST key for cache, populate detail from the response.
- **PPh-Final-not-posted is a 422 SOFT-BLOCK, not a 2xx-warning** → close FAILS, period stays open, FE does NOT flip the chip. NOT a `{data, warnings[]}` two-phase flow ([[two-phase-warn-ack-pattern]] does NOT apply to close).
- **`status` enum discrepancy:** `AccountingPeriod.status` enum is strictly `["open","closed"]`, but `GET /accounting/periods/year/{year}`'s `locked` field description references a `"locked"` status ("every one is in `locked` status"). Either BE returns an under-documented 3rd value or "locked" is year-end prose for "closed". Chip map must defensively handle an unexpected `"locked"` without crashing. FLAG to BE for LNS-378.
- **Year-level infra (shared per PRD):** `POST /accounting/periods/close-year` {year, retained_earnings_account_id?} + Idempotency-Key; `POST /accounting/periods/reopen-year` {year, confirmation_token (= `closing_journal_created_at` verbatim), reason (min 10)} + Idempotency-Key; `GET /accounting/periods/year/{year}` → {year, periods[], close_journal_id?, closing_journal_created_at?, locked}. These are LNS-378; design periods entity/model/repo to be reused.
- **NAMING-COLLISION risk:** legacy client-derived tax-period selector ([[tax-periods-no-be-resource-v1]], `use-list-tax-periods.ts`, `YYYY-MM` strings) is a DIFFERENT concept from this server-backed `AccountingPeriod`. Do NOT reuse the tax-period hook/constants for LNS-377.
- **Capability gating, final v1 answer:** ACCOUNTANT/admin actor-role distinction was explicitly deferred to v2 ([[accounting-prd-lock-2026-05-12]]); v1 account model carries only `role` (product-feature gating, "Determines which product features are enabled") + `features[]`. No `MANAGE_*` capability on FE account/session model (same as LNS-380 [[account-settings-contract-2026-06-24]]). Visibility proxy = `hasFeature("accounting")`; true close/reopen enforcement = BE 403. Render reopen for all accounting-enabled users; degrade on 403.

**⚠️ BE CONTRACT DRIFT — re-fetched 2026-06-25 LNS-377 Phase-7 (spec 593KB→604KB, BE shipped mid-build, ~50 min after Phase-2). `AccountingPeriod` schema RESTRUCTURED. Use THESE field names, NOT the 2026-06-22/Phase-2 ones above:**
- **RENAMED:** `start_date`→**`start_at`** (now date-time not date), `end_date`→**`end_at`** (date-time), `closed_by_user_id`→**`closed_by`**. The earlier names are STALE — LNS-377 shipped reading the old names; DRIFT-1 micro-fix remapped to the new ones (silent blank-`label` break: defensive `?? ""` hid it from tsc/lint/build; caught only by Phase-7 key-by-key re-read — Rule #15 / [[reread-before-handoff-assertion]] validated).
- **NEW fields:** `reopened_count` (int), `updated_at` (date-time), and **`can_close` / `can_reopen` (booleans)** — the per-period capability signal Phase-2 said was ABSENT now EXISTS. v1 FE still degrades via 403 (functionally fine); pre-computing gating off can_close/can_reopen is a PM-routed UX enhancement, not a bug.
- **`status` enum now 3-state `["open","closed","locked"]`** (`locked` = year-end-locked, resolving the Phase-2 discrepancy above — it IS a real 3rd value). reopen now returns 409 **`PERIOD_LOCKED`** (distinct from `PERIOD_NOT_CLOSED`, details `{period_id}`) → "use reopen-year flow". Defensive `isClosed = status!=="open"` renders the chip fine; the reopen-409 copy gap (mapping PERIOD_LOCKED to the wrong "masih terbuka" message) is deferred to LNS-378.
- **`PPH_FINAL_NOT_POSTED` is now DUAL-natured** (reverses Phase-2's "422 soft-block only / prose-only"): (a) **2xx non-blocking warning** on close 200 PRE-deadline (close SUCCEEDS; 200 = `allOf:[AccountingPeriod,{warnings[]}]`, warning item `{code,message,details}`, PPh details NOW fully schema-documented = `{period, tenant_regime, expected_account_code "8110", period_dpp, setor_deadline}`); (b) **422 hard-block** POST-deadline. close 200 period fields are TOP-LEVEL in the allOf (NOT `{data}`-wrapped) so `fromJson(result)` works; reopen 200 = bare `AccountingPeriod`. v1 FE swallows the 2xx `warnings[]` — acceptable per finalized ACs; surfacing richly is a PM enhancement.
- **409/422 now carry documented details:** close 422 `PERIOD_NOT_DRAINED` `{unacked_count}`; close 409 `PERIOD_ALREADY_CLOSED` `{period_id}`.
- **UNCHANGED & re-verified clean:** `id`/`account_id`/`kind`/`status`/`closed_at`/`created_at` keys; page-IN/page-OUT; `Idempotency-Key` required both close+reopen; meta `total_pages→totalPages`.
- **Lesson:** Phase-7 re-fetch was load-bearing — BE shipped field-rename + capability-add + PPh-transport change inside a ~50-min build window. ALWAYS re-fetch at Phase-7 even when Phase-2 was thorough and every static gate is green; defensive model defaults RAISE this obligation (they hide renamed keys).
