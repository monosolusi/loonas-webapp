---
name: opening-balance-post-contract-2026-06-25
description: LNS-379 verified — POST /accounting/opening-balance live contract, idempotency REQUIRED, structured 422, NO is_header on CoaAccount, code 3300 rejected / 3200 correct
metadata:
  type: project
---

LNS-379 Phase-2 verified the live `POST /accounting/opening-balance` contract (operationId `postOpeningBalance`) against `https://dev-api.loonas.id/openapi.json` on 2026-06-25.

**Request:** headers `Idempotency-Key` (**REQUIRED**, pattern `^[A-Za-z0-9_-]{8,255}$`; missing/malformed → 400 `IDEMPOTENCY_KEY_REQUIRED`) + optional `api-version` (default `v1`). Body `{as_of: "YYYY-MM-DD", lines: [{account_id: uuid, debit: number>=0, credit: number>=0}]}`, minItems 1; exactly one of debit/credit > 0 per line.

**Response:** `201 -> bare JournalEntry` (NO `{data}` wrapper, NO warnings envelope) → reuse `JournalModel.fromJson(result)` directly (NOT `JournalWriteResultModel`, which is the journal-create envelope path). JournalEntry has the LNS-369 audit fields (is_reversal, reversed_journal_id, superseded_by_id, is_reversed_currently, posted_by).

**Error codes (resolved E1/E3/E4):**
- 409 `OPENING_BALANCE_EXISTS` (already posted) / `OPENING_BALANCE_BLOCKED` (standard journal ≤ as_of; details.earliest_posted_date).
- 422 `OPENING_BALANCE_UNBALANCED` (details debit_total/credit_total/delta), `_ACCOUNT_NOT_FOUND`, `_HEADER_ACCOUNT` (details account_id+account_code), `_RETAINED_EARNINGS` (**code 3300 rejected; use 3200** — confirms E5, locked), `_NONBALANCE_ACCOUNT` (details account_type), and **`NORMAL_BALANCE_HINT` IS structured** — `details.lines[{account_id, entered_side, corrected_side}]` (E4 resolved: spec types it structured, NOT generic; PM's belief was wrong). FE access path `serverError.details.details.lines` via shipped `parseNormalBalanceHintLines`.

**E2/R1 — header derivation + parentId DRIFT (Phase-3 supersedes Phase-2):** `CoaAccount` has NO `is_header` field (only id/code/name/type/is_system/parent/balance/total_*), BUT `parent` is a nested `{id}` back-ref, so headers ARE derivable client-side = "any account whose `id` is referenced as another account's `parent.id`" by scanning the FULL list. **This OVERRIDES the Phase-2 "FE cannot pre-exclude headers" conclusion** — it can, restoring AC-10. CAVEAT: `LedgerAccountModel.fromJson` reads flat `data["parent_id"]` while the live spec returns nested `parent: {id}` (response) AND `parent` (write body) — genuine drift; `parentId` is null for ALL accounts today. **Must fix the model to read `data["parent"]?.["id"] ?? null` before derivation works.** UID's `parentId === null` predicate is doubly wrong (drift + top-level≠header — a top-level account with no children is a leaf, not a header). Picker must fetch the FULL CoA list (high limit; list paginates default 100). Income-statement types still excluded by type; 3200/3300 by code. The 422 `OPENING_BALANCE_HEADER_ACCOUNT` fallback stays as defense-in-depth. See [[project_coa_account_no_isheader]] (FE AccountType enum + CoaAccount type enum now ALIGNED).

**R2 — 3200 residual is NOT auto-derived (Phase-3):** the POST prose requires `lines` to balance (Σdebit==Σcredit) and only *rejects* code 3300 (hint→3200); there is NO server-side balancing-residual computation. (The spec `residual`/`retained_earnings` hits are unrelated: fixed-asset depreciation; year-close override resolving to 3300.) **FE MUST compute + inject the 3200 line** so the payload balances. Residual (IDR integer) = Σ(asset/contra-asset debits) − Σ(liability/equity/contra-equity credits) over real inputs; >0 → 3200 credit, <0 → 3200 debit (deficit dead-end, BE rejects equity-on-debit). The plain `isBalanced` gate must run AFTER injecting the 3200 line. The wizard needs the 3200 account's UUID → resolve it from the CoA list by `code === "3200"` (identifier-reachability per Rule #18).

**Idempotency-Key wiring already proven** at `journal.ts:52` — pass via `HttpRequest` `config.headers`; live in a provider `useRef`, rotate ONLY on terminal error (journal-create-provider.tsx:69,154 precedent). See [[project_journal_write_contract_2026_06_15]] for the create-journal precedent the POST usecase mirrors (but opening-balance has NO warn/ack two-phase — bare success/fail). use-post-opening-balance uses `useSWRMutationClerk`, hook-local key literal (NOT swr-keys.ts), provider revalidates `GET_OPENING_BALANCE` post-success ([[feedback_mutation_hook_skill_precedence]]). [[project_no_whatsapp_url]] still holds: `LOONAS_WHATSAPP_URL=""` TODO(LNS-344) unresolved; deficit block degrades gracefully (button disabled).
