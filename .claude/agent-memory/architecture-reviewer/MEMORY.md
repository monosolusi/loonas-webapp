# Architecture Reviewer Memory

- [Co-located sub-component anti-pattern](feedback_colocated_subcomponent_antipattern.md) — Engineers put private JSX-returning helpers inside parent files; flag as one-component-per-file violation
- [Retained file name flag](feedback_retained_name_flag.md) — When sub-components keep old parent-widget name prefix after refactor, flag as minor naming drift for EL consideration
- [Empty params class anti-pattern](feedback_empty_params_class_pattern.md) — Empty `class XxxUseCaseParams {}` is a should-fix; use `UseCase<ReturnValue>` with void default instead
- [Text-link button h-11 exception](feedback_button_h11_exception.md) — `h-11` does not apply to text-link-styled `<button>` elements; only to form-interactive controls
- [Inline style tab indicator](feedback_inline_style_tab_indicator.md) — Sliding tab indicator via `style` prop + imperative DOM mutation is a Rule 13 violation; use CSS custom props via className
- [Feature imports app layer](feedback_feature_imports_app_layer.md) — `features/**/presentations/**` must never import from `@/app/`; promote shared components to `core/` first (found in LNS-375 buku-besar-viewer)
- [CSS-hidden dual-branch singleton semantics](feedback_css_hidden_dual_branch_singleton.md) — CSS `hidden`/`sm:hidden` dual-layout keeps BOTH branches mounted; sweep the whole diff for ANY duplicated singleton element (`autoFocus`, `aria-live`/`role=status`, unique `id`, `aria-labelledby`), not just the first found (LNS-364: caught autofocus, missed footer aria-live)
- [LNS-371 review learnings](project_lns371_review_learnings.md) — both recorded accounting debts CLEARED as of 2026-08-30 (mutation keys constant-backed; no source imports repo params); create-page providers still need no guarantee pattern — grep before citing
- [Presentation imports data/models Blocker](feedback_presentation_imports_data_models.md) — app/ and presentations/ must never import from data/models/; only domain/sources/ has that exemption (found LNS-379 provider)
- [Use-case private method must not return DataState](feedback_usecase_private_method_datastate.md) — private methods throw on failure and return plain types; returning DataState from a private method is a Major violation (found LNS-379)
- [Idempotency-Key belongs in use case, not service layer](feedback_idempotency_key_layer.md) — `crypto.randomUUID()` inside ServiceImpl is a Major violation; key must be generated in UseCase.execute() and threaded through params (found LNS-117)
- [Provider empty-state gap](feedback_provider_empty_state_gap.md) — If provider shellState never emits "empty" but impl checks for it, EmptyBody is dead code; Minor finding (found LNS-376 calk)
- [Dead sub-state components](feedback_dead_substate_components.md) — Engineers scaffold loading/error/incomplete sub-state files then handle states inline in the body, leaving the sub-state files unused (found LNS-347 recommended-price block)
- [Inline style grid-template-columns](feedback_inline_style_grid_columns.md) — inline `style` prop for CSS grid-template-columns is a Rule 13 violation; use a CSS custom prop + Tailwind arbitrary class (found LNS-347 profitability table)
- [Centralize predicate with message helper](feedback_centralize_predicate_with_message_helper.md) — when a shared helper centralizes error-message mapping, check the instanceof/code-matching predicate at call sites was centralized too, not just the message (found LNS-457)
- [LNS-457 review learnings](project_lns457_review_learnings.md) — uncommitted work shows empty in `dev...HEAD` diff (check `git status`); fixed-cost providers importing from features/accounting is established precedent; single-consumer provider state has a real coupling constraint, not just an oversight
- [revalidateSWRKey throws in catch](feedback_revalidate_swr_key_throws_in_catch.md) — SWR mutate() defaults throwOnError:true; unguarded await inside a not-found recovery catch OR after a success toast/close (new LNS-564 variant) can misfire recovery or leak stale error state into the next dialog open
- [SectionCard fetch-error convention](project_sectioncard_fetch_error_convention.md) — established pattern is text-error-* + ExclamationCircleIcon + retry button; text-warning-* + ExclamationTriangleIcon + no retry is a convention deviation (found LNS-489, Minor)
- [LNS-564 tiered-pricing review learnings](project_lns564_review_learnings.md) — new `domain/helpers/` precedent (pure calc, no violation but novel taxonomy); absent-vs-empty nullable-aggregate done right; exemplary money/quantity test rigor; one Major (revalidate ordering in two new dialogs)
- [LNS-570 review learnings](project_lns570_review_learnings.md) — I/O-orchestrating app-utils correctly disqualified from `domain/helpers/` (side effects + presentation-typed param, not just "takes an entity"); sweep twin implementation-components sharing one display component for the same unfixed defect shape; getter-derived-from-getter is the canonical drift-proof entity pattern
- [LNS-572 review learnings](project_lns572_review_learnings.md) — sole-writer/sole-reader sentinel idiom confirmed twice (create + detail pages), verify via whole-tree grep not just the doc comment; single-guard boolean flag arg inside a `.map()` isn't an SRP violation on its own; spot-check "wire-identical" claims against the actual HTTP layer instead of trusting the docstring
- [Headless UI Dialog unmounts when closed](feedback_headless_ui_dialog_unmounts_when_closed.md) — reject the "`unmount` defaults false so a closed Dialog stays as display:none" claim; the wrapping Transition defaults `unmount=true` and removes the subtree, so always-mounted worries (dup focus trap, live `role=alert`) are moot; BUT the ~200ms leave window is real — a nullable item prop blanks the panel body mid-fade, fix with a latch at the dialog, not a nullable shared helper
- [Warning callout token convention](feedback_warning_callout_token_convention.md) — house warning callout is `border-warning-400 bg-warning-50` + `text-warning-500`; `text-warning-400` body text fails WCAG AA at ~3.4:1 and is icon/hover-only in-repo
- [Single-consumer list-impl is not the deprecated monolith](feedback_list_impl_single_consumer_not_deprecated.md) — don't flag a new `*-list-impl.tsx` holding only local list state with one child; a `_providers/` wrapper there would violate provider data locality
- [Nav icon alt-text + sibling-collision check](project_nav_icon_alt_text_and_sibling_collision_check.md) — `NavigationGroup`/`NavigationItem` set `Image alt={label}` next to visible text on all ~13 nav icon pairs (pre-existing, fix once in the two shared components); grep method for checking nav-sibling icon-collision claims
- [Inline link base color convention](feedback_inline_link_base_color_convention.md) — the prevailing `text-primary-300` link base fails AA at 3.98:1; `text-primary-400` (6.61:1) is correct for new links — never flag it as drift, and compute contrast before citing a color convention
- [Negative-stock review learnings](project_negative_stock_review_learnings.md) — no-shared-component judgment validated when predicate is getter-centralized but render shapes differ; removed footer button relocated (not dropped) — grep target route before flagging reachability regression; loading-state flash on gated static copy is a real, checkable Minor
- [PR#227 registration-captcha review learnings](project_pr227_registration_captcha_review_learnings.md) — Clerk captcha DOM facts (`#clerk-captcha`, invisible-fallback hard-block, placement) verified via Context7 `/clerk/clerk-docs`; derived-invariant-getter rule carve-out when two call sites need different info (distinct error codes) from the same source facts, not the same predicate
- [Icon alt-text beside visible label generalizes](feedback_icon_alt_text_beside_visible_label_generalizes.md) — the nav-icon alt="" rule isn't nav-specific; any Image icon adjacent to a visible text label should get `alt=""`, not a more-accurate per-variant string (found `AccountTypeCard`, onboarding review)
- [_components/ cross-route locality violation](feedback_components_cross_route_locality_violation.md) — a page's `_components/` is implicitly route-private; a sibling route importing from it is a real finding — verify via grep-all-consumers, fix is promote to nearest common ancestor `_components/`, not feature-level (found `go-to-sign-in.tsx` importing from `kyc-summary/_components/`)
- [KYC re-entry review learnings](project_kyc_reentry_review_learnings.md) — new derived-invariant-getter miss ON THE ENTITY itself (not a call site) even though the PR was fixing that exact drift class; verify "load bearing" docstring claims by grepping the consumer's actual mount points; 3-for-3 confirming example of the domain/helpers input-type disqualifier
- [One-way latch driving derived visibility](feedback_oneway_latch_derived_visibility.md) — a "derive, don't dismiss" notice can lie if its source boolean only resets via one specific writer; trace a type→delete-to-empty→retype cycle, not just the single forward transition
- [F9 nationality-reset review learnings](project_f9_nationality_reset_review_learnings.md) — two-tier provider split (wizard buffer vs. page-level submit-state); plain-hook-to-provider is a recurring defect class in this codebase, not a one-off; `ReturnType<typeof useX>` as the whole context value is new precedent (existing pattern nests it under a field)
- [SWR error + stale data false alarm](feedback_swr_error_stale_data_false_alarm.md) — when a diff starts reading a SWR hook's `error`, check the data-populated-plus-error state: SWR keeps `data` on a failed revalidation and `isLoading` stays false, so a "failed" message renders over a working list and any `isLoading`-keyed pending state never fires — use `isValidating`
- [F10 WNA review learnings](project_f10_wna_review_learnings.md) — read-path union + write-path predicate over ONE catalogue is not LNS-608 drift (tells: independent derivations + one unused); a union guards a reason's presence but never `""`, so check for a runtime copy guard in a suite-reachable `.ts`; `opacity-50` on the same node as its own fill cancels the fill
- [LNS-741 review learnings](project_lns741_review_learnings.md) — clean cash-entry cancel-by-reversal diff; confirms `details.status` (never `httpCode`) rotation rule held under real pressure; one recurring duplicated-unwrap Minor between provider catch and its own `_utils/classify-*` helper — see [[feedback_centralize_predicate_with_message_helper]] (now covers single-provider case too)
- [LNS-739 review learnings](project_lns739_review_learnings.md) — `shellState` "empty" swallows `pageError` under `keepPreviousData`; `cost-valuation-gaps` onRetry is the unguarded-`mutate()` template that propagates by copy
- [LNS-738 review learnings](project_lns738_review_learnings.md) — `refresh: () => mutate()` with `KeyedMutator` typing is a false positive (check the declared type, not the arrow); comments naming endpoint→error-code mappings must be diffed per-endpoint against the live spec (DIRECTION_MISMATCH is create-only; update/delete reject with 409 REFERENCED)
- [Data model one-per-file + toEntity-only](feedback_data_model_one_per_file.md) — PR 246 convention: nested models get own file; entity-backed models convert via toEntity() only; widening-sweep method
- [LNS-743 review learnings](project_lns743_review_learnings.md) — details.code unwrap validated; classify-fetch-error now in core/helpers; resolver-extraction is the fix shape for "too many ifs"; cross-resolver invariant test bridge is not a coupling smell
- [Error copy names real recovery](feedback_error_copy_names_real_recovery.md) — swr-provider shouldRetryOnError makes "no retry button" a wait-state, not a defect; but copy must not name an action the code doesn't perform (modal toggle never re-mounts the hook owner)
- [LNS-740 review learnings](project_lns740_review_learnings.md) — cash-entry create provider is the corrected submit+idempotency template (journal-create twin still carries both anti-patterns); registry-absent code literals OK pending second consumer; UNKNOWN-unwrap now 6 repo copies; search-combobox opacity-50 debt
